package com.svalero.bookingexam.feature.login;

import com.svalero.bookingexam.data.User;
import com.svalero.bookingexam.feature.search.SearchContract;

public class LoginModel implements LoginContract.Model {
    @Override
    public void getUserService(OnLoginUserListener onLoginUserListener, User user) {

    }
}
